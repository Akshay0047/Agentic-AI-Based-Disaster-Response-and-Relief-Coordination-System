[project]
name = "agent-worker-service"
version = "0.1.0"
description = "Agent Worker service (Claude agent + tool executor) for the Disaster Response System"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"